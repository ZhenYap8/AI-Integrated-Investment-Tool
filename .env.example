# Copy to IBBack/.env for local dev, or set in Render Dashboard → Environment

# Backend (sudut-invest-api service)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1
NEWS_API_KEY=

# Frontend (sudut-invest-web service) — set at build time
# VITE_API_URL=sudut-invest-api.onrender.com
# (Render blueprint sets host automatically; https:// is added at runtime)
